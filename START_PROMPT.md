Initialize this project as `TAKI HOUSE – WEB TECH`.

Read and follow `AGENTS.md` as the highest project-level operating instruction for website technical work.

Then perform task `WEB-BOOT-001 – Codex Technical Baseline` in READ-ONLY mode.

Objectives:
1. Confirm you have loaded AGENTS.md.
2. Inventory what access is actually available:
   - public web
   - repository/source
   - terminal/local tools
   - staging
   - production
3. Do not request or expose passwords, OTPs, API keys or secrets in chat.
4. Check only safe public baseline items:
   - homepage HTTP/HTTPS reachability
   - robots.txt
   - sitemap discovery
   - visible canonical on homepage
   - visible meta robots on homepage
   - obvious redirect behavior
   - obvious 4xx/5xx from the tested URLs
5. Do not crawl aggressively.
6. Do not submit forms.
7. Do not change WordPress, plugins, theme, cache, CDN, DNS, database or tracking.
8. Report missing access required for later technical work.
9. Return the result using the reporting format defined in AGENTS.md.

End with:
- READY / NOT READY for read-only technical tasks
- READY / NOT READY for staging changes
- READY / NOT READY for production changes
- exact reason for each status
